# Luc's dot- and config files
As you might have guessed ...
## Installation
It does not make much sense to blindly install someone else's configs but you
can do it anyway:
```shell
git clone https://github.com/lucc/config.git ~/.config
cd !$
make
```
I use `~/.config` but thats just taste.
