# mutt settings for gmx.de mail account.
# vim: ft=muttrc foldmethod=marker

set smtp_url=$my_gmx_smtp_url
set from=$my_gmx_from
#color status $my_gmx_fg $my_gmx_bg
set status_format="${my_default_status_format} ${my_gmx_status_string}"
